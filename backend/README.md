# Backend (NestJS)

Ce dossier contient l’API NestJS servant de proxy et logique métier pour SmartPredict MLOps.

## Démarrage rapide

```bash
cd backend
npm install
npm run start:dev
```

L’API sera disponible sur http://localhost:3001
